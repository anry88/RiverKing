# Screenshot Notes

This directory contains showcase images used by the repository README.

The current set is intentionally stored as stable repository assets. If you have access to the live Telegram product and want literal product captures instead, replace the files in place and keep the same names.

Current target set:

- `01-fishing-home.png`
- `02-guide-achievements.png`
- `03-tournaments-current.png`
- `04-club-overview.png`
- `05-shop-and-daily.png`
- `06-ratings.png`
- `07-bot-flow.png`

Android store capture set:

- `android-01-daily-reward.png`
- `android-02-fishing-home.png`
- `android-03-leaders.png`
- `android-04-catalog.png`
- `android-05-club.png`
- `android-06-shop.png`

These Android screenshots were captured from the running emulator after installing the current `directDebug` build. Keep them as the baseline store/gallery set until a newer visual refresh or feature change makes them stale.

itch.io-compatible downscaled copies:

- `itch/android-01-daily-reward.png`
- `itch/android-02-fishing-home.png`
- `itch/android-03-leaders.png`
- `itch/android-04-catalog.png`
- `itch/android-05-club.png`
- `itch/android-06-shop.png`

These `itch/` copies preserve the same portrait captures but are downscaled to `968x2160` so they stay within itch.io's `3840x2160` upload limit. Use the original `1280x2856` files for Google Play and the `itch/` copies for itch.io.

Public repository note:

- Admin-side flows are intentionally excluded from the public screenshot set.
- If you ever need a private operator deck, keep those assets separate from this folder.

When these assets are refreshed, keep the naming stable so the README does not need structural changes.
